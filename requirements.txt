# Core data + ML stack (all free, open-source)
pandas>=2.2                # 2.2+ needed for the 'ME' month-end offset alias
numpy>=1.24
scikit-learn>=1.3
yfinance>=0.2.40          # free NSE/global price data
matplotlib>=3.7           # equity-curve plots
requests>=2.28            # free GDELT news-sentiment fetcher (no API key)

# Optional but recommended (the workhorse model). Falls back to sklearn if absent.
lightgbm>=4.0

# Optional: cointegration filtering for pairs trading (run_pairs.py --cointegration)
# statsmodels>=0.14

# Optional: only needed if you enable the free-LLM news re-scoring layer.
# Pick ONE provider; none are required for the baseline or the GDELT overlay.
# groq>=0.9              # Groq free tier (fast). Set GROQ_API_KEY.
# google-generativeai    # Gemini free tier. Set GEMINI_API_KEY.
# (Ollama uses 'requests', already included above — fully free/offline.)
